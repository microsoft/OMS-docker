version: 1.0-preview-1
steps:
  - build: -t {{.Run.Registry}}/test/citest:{{.Run.Date}}-{{.Run.ID}} .
    workingDirectory: ci_feature
  - push: ["{{.Run.Registry}}/test/citest:{{.Run.Date}}-{{.Run.ID}}"]